# LeadGen secret references (NO secret values).
# Values must be AWS Secrets Manager Secret IDs/ARNs.
#
# DB secret JSON schema (SecretString):
#   required: host|endpoint, username|user, password
#   optional: port (default 5432), dbname (default leadgen), sslmode (default require)
#
# Intake API key secret JSON schema:
#   required: api_key OR key
#
leadgen_db_secret_id: "arn:aws:secretsmanager:us-east-2:703671899211:secret:motorcade/prod/leadgen-db-fdEKIO"
leadgen_intake_api_key_secret_id: "arn:aws:secretsmanager:us-east-2:703671899211:secret:motorcade/prod/leadgen-intake-api-key-EOba1L"
